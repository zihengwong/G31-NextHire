<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#FFFFFF">

    <!-- 🏷️ Title -->
    <TextView
        android:id="@+id/applied_jobs_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="My Applications"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="12dp" />

    <!-- 📋 RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/applied_jobs_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/applied_jobs_title"
        android:layout_marginBottom="80dp"
        tools:listitem="@layout/job_item"
        android:visibility="visible" />

    <!-- ❗ Empty State -->
    <LinearLayout
        android:id="@+id/empty_state_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/applied_jobs_title"
        android:layout_marginBottom="80dp"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp"
        android:visibility="gone">

        <ImageView
            android:id="@+id/empty_applied_icon"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/ic_bookmark_border"
            android:tint="#AAAAAA" />

        <TextView
            android:id="@+id/empty_applied_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No applications yet."
            android:textColor="#777777"
            android:textSize="16sp"
            android:layout_marginTop="12dp" />
    </LinearLayout>

</RelativeLayout>
